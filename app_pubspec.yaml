name: mixcontrol
description: X32 MixControl — unofficial remote for Behringer X32/M32 over OSC/UDP.
publish_to: "none"
version: 0.5.1+6

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
